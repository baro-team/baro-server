spring:
  application:
    name: redispatch-service

server:
  port: ${PORT:8083}

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

baro:
  openapi:
    title: Redispatch Service API
    description: BARO redispatch-service API documentation
    version: v1
